{
  "name": "agd-master",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend-react -- --host\"",
    "install-all": "npm install --prefix backend && npm install --prefix frontend-react"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
